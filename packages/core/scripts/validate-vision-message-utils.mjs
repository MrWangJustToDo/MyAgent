/**
 * @deprecated Use validate:capability-message-utils.
 * Thin wrapper kept so existing docs/scripts that reference the old name keep working.
 */
import "./validate-capability-message-utils.mjs";
