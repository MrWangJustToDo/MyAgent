/**
 * Placeholder apiKey for remote provider adapters (server injects the real key).
 *
 * Re-exported from `@codent/core` (`REMOTE_PROVIDER_API_KEY`) so this file stays
 * a single compatibility point for server-side imports.
 */
export { REMOTE_PROVIDER_API_KEY } from "@codent/core";
